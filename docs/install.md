# Install

MBT could be obtained from PyPI using your favourite Python package manager:

```sh
pipx install microbuildtool
```

## Prerequisites

In order to run, MBT requires **exactly** Java 8 SDK installed on your machine,
as it is the last JDK to support Java 1.1 as target.
